{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentcert.dev/schemas/agentcert-evidence.schema.json",
  "title": "AgentCertEvidence",
  "description": "A single evidence finding. Required fields make the finding auditable; optional fields link it to sources, artifacts, remediation, and engine-specific metadata.",
  "type": "object",
  "additionalProperties": true,
  "required": ["id", "kind", "severity", "message"],
  "properties": {
    "id": {
      "type": "string"
    },
    "kind": {
      "type": "string",
      "description": "policy_violation, assertion_result, trace_diff, runtime_warning, or audit_event."
    },
    "severity": {
      "type": "string",
      "enum": ["critical", "high", "medium", "low", "info"]
    },
    "message": {
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "artifactPath": {
      "type": "string"
    },
    "suggestedFix": {
      "type": "string"
    },
    "metadata": {
      "type": "object",
      "description": "Optional engine-specific context such as scenario name, fault name, policy id, or verification details.",
      "additionalProperties": true
    }
  }
}
