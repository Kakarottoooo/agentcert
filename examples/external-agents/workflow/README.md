# Workflow agent

```bash
npx agentcert init --template workflow --subject claims-workflow
node agentcert.adapter.mjs
```

Instrument durable state transitions, approval decisions, external effects, and observed-outcome verification.
