# Coding agent

```bash
npx agentcert init --template coding --subject coding-agent
node agentcert.adapter.mjs
```

Call the envelope boundary when a repository-changing action is proposed or verified. Do not transmit source files by default.
