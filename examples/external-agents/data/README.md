# Data agent

```bash
npx agentcert init --template data --subject analytics-agent
node agentcert.adapter.mjs
```

Record query execution and verification metadata. Keep raw datasets and credentials outside evidence unless explicitly approved.
